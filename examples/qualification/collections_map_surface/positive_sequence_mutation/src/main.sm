fn main() {
    let mut values: Sequence(i32) = [1];
    values = push(values, 2);
    values = prepend(values, 0);
    assert(len(values) == 3);
    assert(values[0] == 0);
    assert(values[1] == 1);
    assert(values[2] == 2);
    return;
}
