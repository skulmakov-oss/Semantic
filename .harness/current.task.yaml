task:
  id: VM-CORE-QUAD-SEMANTIC-MISMATCH-AUDIT
  title: Audit quad opcode semantic mismatch
  type: docs
  mode: active

intent:
  summary: docs(vm/core-quad): audit quad opcode semantic mismatch
  issue: "#1440"

layer:
  name: core_quad
  owner: sm-vm

scope:
  allowed_paths:
    - docs/roadmap/core_quad/vm_core_quad_semantic_mismatch_audit.md
    - .harness/current.task.yaml
    - .harness/reports/VM-CORE-QUAD-SEMANTIC-MISMATCH-AUDIT.md

  forbidden_paths:
    - crates/**
    - src/**
    - tests/**
    - tools/**
    - README.md
    - Cargo.toml
    - Cargo.lock

actions:
  allowed:
    - read
    - edit_allowed_paths
    - push
    - create_pr

  forbidden:
    - write_rust
    - change_opcode_semantics

verification:
  required:
    - git diff --check
    - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/harness-check.ps1
    - git status --short

report:
  output: .harness/reports/VM-CORE-QUAD-SEMANTIC-MISMATCH-AUDIT.md