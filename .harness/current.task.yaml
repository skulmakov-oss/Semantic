task:
  id: CORE-QUAD-VM-LATTICE-TRUTH-OPCODE-CONTRACT
  title: Define lattice vs truth-table opcode contract
  type: docs
  mode: active

intent:
  summary: docs(core-quad/vm): define lattice vs truth-table opcode contract
  issue: "#1442"

layer:
  name: core_quad
  owner: sm-vm

scope:
  allowed_paths:
    - docs/roadmap/core_quad/lattice_truth_opcode_contract.md
    - .harness/current.task.yaml
    - .harness/reports/CORE-QUAD-VM-LATTICE-TRUTH-OPCODE-CONTRACT.md

  forbidden_paths:
    - crates/**
    - src/**
    - tests/**
    - tools/**
    - README.md
    - Cargo.toml
    - Cargo.lock

actions:
  allowed:
    - read
    - edit_allowed_paths
    - push
    - create_pr

  forbidden:
    - write_rust
    - change_opcode_semantics

verification:
  required:
    - git diff --check
    - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/harness-check.ps1
    - git status --short

report:
  output: .harness/reports/CORE-QUAD-VM-LATTICE-TRUTH-OPCODE-CONTRACT.md